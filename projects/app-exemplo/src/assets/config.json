{
    "sentry": {
        "sentryDsn": "#{sentryDsn}",
        "sentryRelease": "#{sentryRelease}",
        "sentryEnvironment": "#{sentryEnvironment}"
    },
    "environment": {
        "app": "app-exemplo",
        "production": true,
        "ambiente": "#{ambiente}",
        "api": "#{api}",
        "apiGoogle": "#{apiGoogle}",
        "cargainicial": "#{cargainicial}"
    }
}
